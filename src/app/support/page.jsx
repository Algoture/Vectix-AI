import { Clock } from "lucide-react";

const page = () => {
  return (
    <div className="min-h-screen cen gap-2">
      Support Page is in Progress
      <Clock />
    </div>
  );
};

export default page;
